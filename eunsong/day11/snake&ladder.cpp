#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

/*bool cmp(const pair<int, int> &a, const pair<int, int> &b){
    return 
}*/

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);

    int n,m;
    cin >> n >> m;

    /*
    vector<pair<int, int> > ladder(n);
    vector<pair<int, int> > snake(m);
    for(int i=0; i<n; i++)
        cin >> ladder[i].first >> ladder[i].second;
    for(int i=0; i<m; i++)
        cin >> snake[i].first >> snake[i].second;
    
    sort(ladder.begin(), ladder.end())*/

    return 0;
}