#include <iostream>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int N;
    cin >> N;
    
    int *A = new int[N+1];
    for(int i=1; i<=N+1; i++)
        cin >> A[i];

    int count = 0;

    
    
    return 0;
}